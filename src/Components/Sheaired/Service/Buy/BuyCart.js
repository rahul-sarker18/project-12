import React from 'react';

const BuyCart = ({p}) => {
    console.log(p);
    const {Price , image , _id} = p;

    return (
        <div>
            
        </div>
    );
};

export default BuyCart;